{
  "scripts": {
      // "heroku-prebuild": "cd packages/front-end && npm install && npm run build"
      "build": "cd packages/front-end && nuxt build",
      "start": "cd packages/front-end && npm run dev"
  }
}
