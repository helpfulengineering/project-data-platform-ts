{
  "name": "e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end UI workflow tests for the Nuxt front end + back end",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
